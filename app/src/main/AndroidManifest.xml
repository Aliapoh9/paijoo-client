<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paijoov1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".chatpressActivity"></activity>
        <activity android:name=".Home_with_name"></activity>

        <activity android:name=".Chat_from_home"></activity>
        <activity
            android:name=".getting_started"
            android:theme="@style/AppTheme" /> <!-- <activity -->
        <!-- android:name=".MainMenu2" -->
        <!-- android:label="@string/title_activity_main_menu2"></activity> -->
        <activity
            android:name=".test_register"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".new_register"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Register"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".homeActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".chatActivity"
            android:theme="@style/AppTheme" /> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login" /> -->
        <activity
            android:name=".First_screen"
            android:theme="@style/AppTheme" />
        <activity android:name=".NewLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ng.com.obkm.bottomnavviewwithfragments.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="ng.com.obkm.bottomnavviewwithfragments.SettingsActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>